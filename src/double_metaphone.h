#ifndef DOUBLE_METAPHONE__H
#define DOUBLE_METAPHONE__H

#include <string>
#include <vector>
#include <array>

// Modified to return a single string instead of an array of two strings
void DoubleMetaphone(const std::string &str, std::string *code);

#endif /* DOUBLE_METAPHONE__H */
